name: 'pcit-plugin-demo'
description: 'pcit plugin demo'
inputs:
  dir:  # id of input
    description: 'vuepress source markdown dir'
    required: true
    default: 'docs'
# outputs:
#   time: # id of output
#     description: 'The time we greeted you'
runs:
  using: 'docker'
  image: 'Dockerfile'
  # args:
  #   - ${{ inputs.who-to-greet }
