version: "3.7"

services:

  pcit:
    build:
      context: .
      dockerfile: Dockerfile
      target: php
      args:
        - PHP_VERSION=${PHP_VERSION:-7.2.12}
        - NODE_VERSION=${NODE_VERSION:-11.1.0}
        - NODE_REGISTRY=https://registry.npm.taobao.org
    image: khs1994/pcit:alpine
