version: "3.7"

services:

  pcit:
    build:
      context: .
      dockerfile: Dockerfile
      target: php
      args:
        - PHP_VERSION=${PHP_VERSION:-7.3.2}
        - NODE_VERSION=${NODE_VERSION:-11.10.0}
        - NODE_REGISTRY=https://registry.npm.taobao.org
    image: pcit/pcit:alpine
