steps:
  script:
    if:
      tag: master

  script2:
    if:
      tag:
      - dev

  # script3:
  #   if:
  #     tag:
  #       k: v

  script4:
    if:
      tag:
        include: master

  script5:
    if:
      tag:
        include:
        - master

  script6:
    if:
      tag:
        exclude: master

  script7:
    if:
      tag:
        exclude:
        - master

  # script8:
  #   if:
  #     tag:
  #       exclude:
  #         k: v
