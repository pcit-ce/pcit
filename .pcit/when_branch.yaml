steps:
  script:
    if:
      branch: master

  script2:
    if:
      branch:
      - dev

  # script3:
  #   if:
  #     branch:
  #       k: v

  script4:
    if:
      branch:
        include: master

  script5:
    if:
      branch:
        include:
        - master

  script6:
    if:
      branch:
        exclude: master

  script7:
    if:
      branch:
        exclude:
        - master

  # script8:
  #   if:
  #     branch:
  #       exclude:
  #         k: v
